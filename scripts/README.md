# Scripts

Use this folder for helper scripts, such as:

- Generate sample data
- Clean sample data
- Export metadata
- Build demo refresh logs

Do not store credentials in scripts. Use environment variables and `.env.example` instead.
