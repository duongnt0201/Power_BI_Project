# SQL Scripts

Use this folder to store sanitized SQL scripts.

Recommended files:

- `data_extraction.sql` - extract data from source tables/views
- `data_cleaning.sql` - cleaning and transformation logic
- `analysis_queries.sql` - analytical queries used for validation or insights

Do not include real credentials, internal server names, or confidential schema/table names.
